{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/SchapiroLabor/micronuclAI_nf/dev/assets/schema_input.json",
    "title": "micronuclAI_nf pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "image": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.(tif|tiff)$",
                "errorMessage": "Image path must be provided, cannot contain spaces and must have extension '.tiff' or '.tif'"
            },
            "segmentation": {
                "type": "string",
                "format": "file-path",
                "exists": false,
                "pattern": "^\\S+\\.(tif|tiff)$",
                "errorMessage": "Image path must be provided, cannot contain spaces and must have extension '.tiff' or '.tif'"
            }
        },
        "required": ["sample", "image"]
    }
}
